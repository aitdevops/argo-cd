apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aitdevops-backend-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/aitdevops/argocd.git'
    targetRevision: backend
    path: charts/aitdevops-backend
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: backend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
